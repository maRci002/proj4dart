name: proj4dart
description: Proj4dart is a Dart library to transform point coordinates from one coordinate system to another, including datum transformations.
# version: 0.0.1
# homepage: https://github.com/maRci002/proj4dart

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  mgrs_dart: ^1.0.0

dev_dependencies:
  pedantic: ^1.8.0
  test: ^1.6.0
